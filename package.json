{
    "scripts": {
        "test": "newman run Herokuapp.postman_collection.json",
        "test-report": "newman run Herokuapp.postman_collection.json --reporters htmlextra"
    },
    "dependencies": {
        "newman": "^5.3.2"
    }
}
